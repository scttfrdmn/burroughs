Phase 4 clause 3's witness guests — provenance, per the wabt precedent. Neither binary is reproducible
from this repository alone: both come from the Phase 4 fork.

Fork tree:    github.com/scttfrdmn/wp-phase4 (local), branch wasmpreempt/phase4
Fork SHA:     cc0e44323ff4859af92bb7cf75d780e85b2f95ba
Built on:     darwin/arm64, 2026-09-25
Guest source: guest.go.txt in this directory (byte-identical for BOTH arms)

  c3_stopped.wasm    — the world IS stopped (unmodified fork)
    GOEXPERIMENT=burroughsspawn GOOS=wasip1 GOARCH=wasm $(fork)/bin/go build -o c3_stopped.wasm .
    sha256: 18e846f073a2a9d3c96d67627396282442969e63caf7a02889ca0b60eb24e1f5
    bytes:   2016232

  c3_notstopped.wasm — the SAME source, built against a fork with TWO edits in
                       src/runtime/proc.go's stopTheWorldWithSema. Same build command.
    sha256: 7866753818601b47ac858a06a904a0e630be5dd23247a69df11623ed5448f665
    bytes:   2015164

    edit 1 — the wait itself:
        wait := sched.stopwait > 0      ->  wait := false
    edit 2 — the double-check that follows it:
        if sched.stopwait != 0 {        ->  if false {
            bad = "stopTheWorld: not stopped (stopwait != 0)"

    **TWO edits were needed, and the second is the point.** Neutering the wait alone leaves that
    double-check, which THROWS. A throw is a crash, and this witness exists for the SILENT failure — so
    with only edit 1 the negative arm would prove that Go's own assertion works rather than that the heap
    corrupts under a world that is not stopped.

REGISTERED READINGS (harness-protocol.md, "CLAUSE 3's WITNESS" and "The positive arm"):
  world stopped      -> code:0 VERIFIED, 10 of 10; 8 collections / 120,000 mutations / 0 finalizers
  world NOT stopped  -> "verification FAILS — checksum mismatch, wrong count, or a finalizer firing"

REPRODUCED, with one DEVIATION recorded rather than smoothed over:
  world stopped      -> 10 of 10 VERIFIED, collected=8, finalized=0, mutations=120000, nodes=4000, bad=0
                        — matches the registered figures exactly.
  world NOT stopped  -> 0 of 3 verified, which is the discrimination the gate needs. But the failure mode
                        is NOT the registered one: the guest DIES before reaching its walk, with a Go
                        runtime traceback through runtime.gcBgMarkStartWorkers (mgc.go:1711) and
                        proc_exit(2), at collected=1 / mutations=120000. So the collector did run and did
                        race the mutator; what it produced here is a crash rather than a surviving
                        corrupted heap.

  Why the deviation is stated and not hidden: the registered reading is about the SILENT case, and a crash
  is a louder one. The reconstruction therefore demonstrates that the witness discriminates a stopped
  world from a not-stopped one, and does NOT demonstrate that it would catch a corruption that left the
  program running. Reporting the second as though it were the first would be the exact substitution this
  witness exists to prevent.

FULL-n REPRODUCTION, 2026-09-25 (the committed test defaults to n=1/1; see its doc comment):
  BURROUGHS_WITNESS_FULL=1 go test ./internal/wasi/ -run TestClause3 -timeout 2100s
  world_stopped      10 of 10 verdict=1 collected=8 finalized=0 mutations=120000 nodes=4000 bad=0
  world_not_stopped   3 of 3  died before verifying, collected=1, mutations=120000, 0 verified
  One run costs ~77s under the interpreter, so the registered n is ~17 minutes. The default run asserts the
  same FIGURES every time and only weakens the RATE; it is not a skip, because both arms always execute.

===============================================================================================
REBUILT 2026-09-26 AT SCALED PARAMETERS, and the binaries above are SUPERSEDED.
===============================================================================================

The figures recorded above (4000 nodes / 120,000 mutations) are the REGISTERED ones and were reproduced
exactly — 10 of 10 VERIFIED — but those binaries are NOT the ones committed here, and their hashes above no
longer resolve to a file in this directory. They are kept as the record of the full-parameter reproduction.

WHY: one run at the registered parameters costs ~77s uninstrumented, and CI runs the suite under `-race`,
where this package is ~9x slower (measured). A positive run would be ~12 minutes and CI's race job has a
25-minute budget for the whole tree; it timed out three arms at once.

COMMITTED binaries, same guest source, scaled structure (500 nodes / 8,000 mutations / 8 collections):
  c3_stopped.wasm
    sha256: d52da0f702f6d6b7832353307a0aada208bbe970d6258b829df7fb77f08edf25
    bytes:   2016352
  c3_notstopped.wasm  (same two stopTheWorldWithSema edits as recorded above)
    sha256: 5fc984cd691baa44af6e26d3a7c09e3ef83130a3dfb85f9a5cfd8417d2925f1f
    bytes:   2015262

  measured: 23.2s uninstrumented, 208.6s under -race, both arms passing.

WHAT IS PRESERVED: the checksum-over-successor shape, the finalizers on reachable nodes, the
mutator/collector race, and both arms' discrimination (verified vs. not).
WHAT IS GIVEN UP: identity with the registered FIGURES in the committed gate. The gate asserts its own
scaled figures exactly (collected=8, mutations=8000, nodes=500, finalized=0, bad=0).

A THIRD SLOT WAS ADDED, and it was earned under -race: 0x0F38 counts collections ENTERED, set before
runtime.GC() rather than after. The vacuity check first read `collected`, which counts COMPLETIONS — so a
guest that died INSIDE the first collection scored 0 and was reported as having exercised nothing, when
dying inside a collection is the not-stopped world biting hardest. Un-raced the negative arm dies after one
collection completes; under -race it dies inside the first.

===============================================================================================
REBUILT 2026-09-26 WITH VERIFIER-LIVENESS MODES. Hashes above are superseded again.
===============================================================================================

  c3_stopped.wasm
    sha256: 8a31ea910e6d290a2f8da3de4475a727b6601240f9081243c754bcc255082328
    bytes:   2140217
  c3_notstopped.wasm  (same two stopTheWorldWithSema edits; rebuilt from the same source)
    sha256: 4717c1c58e873ff18df04083f02bd43aaebfb4b84a83d450fe2c768d9af6497e
    bytes:   2139118

WHY: the verifier had NEVER been watched catch corruption. The not-stopped arm fails by CRASHING, so the
checksum walk itself had only ever passed — live against a crash, unfalsified against the failure it exists
for, a heap corrupted while the program keeps running. Two modes now break the heap deliberately on a
CORRECTLY STOPPED world, selected by argv so one binary serves all three positive-side arms:

  mode "corrupt"    flips a payload in a reachable node WITHOUT re-deriving its sum.
                    Measured: verdict=2, bad=11, nodes=10 — the checksum clause fires and NAMES the node.
  mode "finalizer"  runs the finalizer's BODY on a node the ring still holds.
                    Measured: verdict=2, finalized=1 — the finalizer clause fires.
  no mode           the normal arm, argv byte-identical to before these existed.
                    Measured: verdict=1, collected=8, finalized=0, mutations=8000, nodes=500, bad=0.

HONEST SCOPE OF THE "finalizer" MODE: a real collector will not run a finalizer on a reachable object, and
this does not make one. What is under test is the VERIFIER'S finalizer clause — that a non-zero count is
reported — not the collector's behaviour. A pass means the report works.

SO CLAUSE 3'S CLAIM IS NOW TWO CLAIMS, and they should not be conflated:
  1. the verifier is LIVE against both of its clauses (checksum and finalizer), watched firing; and
  2. the witness separates a stopped world from a not-stopped one, the latter by crashing.
It still does NOT show that a not-stopped world produces a SURVIVING corrupted heap that the walk then
catches — that conjunction has not been observed, and claim 1 plus claim 2 do not add up to it.
